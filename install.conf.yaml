- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig:

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

- shell:
    # - [git submodule update --init --recursive, Installing submodules]
    - command: './homebrew.setup.zsh'
      stdin: true
      stdout: true
      sterr: true